import { readFileSync } from 'fs';
var ConfigFsLoader = (function () {
    function ConfigFsLoader(path) {
        if (path === void 0) { path = '/config.json'; }
        this.path = path;
    }
    ConfigFsLoader.prototype.loadSettings = function () {
        return Promise.resolve(JSON.parse(readFileSync(this.path, 'utf8')))
            .then(function (settings) { return settings; })
            .catch(function () { return Promise.reject('Path unreachable!'); });
    };
    return ConfigFsLoader;
}());
export { ConfigFsLoader };
