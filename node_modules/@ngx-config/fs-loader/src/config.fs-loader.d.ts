import { ConfigLoader } from '@ngx-config/core';
export declare class ConfigFsLoader implements ConfigLoader {
    private readonly path;
    constructor(path?: string);
    loadSettings(): any;
}
