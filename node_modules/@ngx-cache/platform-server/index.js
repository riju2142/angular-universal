import { NgModule, Optional, SkipSelf } from '@angular/core';
export * from './src/fs-cache.service';
var ServerCacheModule = (function () {
    function ServerCacheModule(parentModule) {
        if (parentModule)
            throw new Error('ServerCacheModule already loaded; import in SERVER module only.');
    }
    ServerCacheModule.forRoot = function (configuredProviders) {
        return {
            ngModule: ServerCacheModule,
            providers: configuredProviders
        };
    };
    return ServerCacheModule;
}());
export { ServerCacheModule };
ServerCacheModule.decorators = [
    { type: NgModule },
];
ServerCacheModule.ctorParameters = function () { return [
    { type: ServerCacheModule, decorators: [{ type: Optional }, { type: SkipSelf },] },
]; };
