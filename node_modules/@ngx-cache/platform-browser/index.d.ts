import { InjectionToken, ModuleWithProviders } from '@angular/core';
import { CacheService } from '@ngx-cache/core';
export * from './src/local-storage-cache.service';
export * from './src/memory-cache.service';
export declare const STATE_ID: InjectionToken<string>;
export declare class BrowserCacheModule {
    private readonly stateId;
    private readonly cache;
    static forRoot(configuredProviders?: Array<any>): ModuleWithProviders;
    constructor(parentModule: BrowserCacheModule, stateId: string, cache: CacheService);
    private getCacheValue(defaultValue);
}
