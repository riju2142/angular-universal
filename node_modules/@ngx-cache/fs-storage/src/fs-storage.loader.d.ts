export declare abstract class FsStorageLoader {
    readonly abstract path: string;
    readonly abstract quota: number;
}
export declare class FsStorageStaticLoader implements FsStorageLoader {
    private readonly storagePath;
    private readonly storageQuota;
    constructor(storagePath?: string, storageQuota?: number);
    readonly path: string;
    readonly quota: number;
}
