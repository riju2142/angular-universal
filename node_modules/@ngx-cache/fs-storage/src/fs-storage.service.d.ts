import { Storage } from '@ngx-cache/core';
import { FsStorageLoader } from './fs-storage.loader';
export declare class FsStorageService extends Storage {
    loader: FsStorageLoader;
    length: number;
    keys: Array<string>;
    private readonly instances;
    private readonly path;
    private readonly quota;
    private readonly pid;
    private metadata;
    private bytesUsed;
    constructor(loader: FsStorageLoader);
    setItem(key: string, value: any): boolean;
    getItem(key: string): any;
    removeItem(key: string): boolean;
    key(index: number): any;
    clear(): boolean;
    private getStats(key);
    private deleteDirectory(dirPath);
    private deletePath(path);
    private deleteInstance();
}
