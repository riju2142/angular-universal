import { CacheOptions } from './models/cache-options';
export declare abstract class CacheLoader {
    readonly abstract key: string;
    readonly abstract options: CacheOptions;
}
export declare class CacheStaticLoader implements CacheLoader {
    private readonly cacheKey;
    private readonly cacheOptions;
    constructor(cacheKey?: string, cacheOptions?: CacheOptions);
    readonly key: string;
    readonly options: CacheOptions;
}
