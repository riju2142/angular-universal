import { Routes } from '@angular/router';
export declare abstract class I18NRouterLoader {
    abstract loadTranslations(): any;
    abstract getRoutes(): Routes;
    abstract getTranslations(): any;
}
export declare class I18NRouterStaticLoader implements I18NRouterLoader {
    private readonly routes;
    private readonly translations;
    constructor(routes?: Routes, translations?: any);
    loadTranslations(): any;
    getRoutes(): Routes;
    getTranslations(): any;
}
