import { PipeTransform } from '@angular/core';
import { I18NRouterService } from './i18n-router.service';
export declare class I18NRouterPipe implements PipeTransform {
    private readonly i18nRouter;
    constructor(i18nRouter: I18NRouterService);
    transform(value: string | Array<any>): string;
    private translateQuery(value);
}
