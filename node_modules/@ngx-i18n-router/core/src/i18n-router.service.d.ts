import { Router, Routes } from '@angular/router';
import { I18NRouterLoader } from './i18n-router.loader';
export declare const ROOT_ROUTE_PREFIX = "ROOT";
export declare class I18NRouterService {
    loader: I18NRouterLoader;
    private readonly router;
    languageCode: string;
    useLocalizedRoutes: boolean;
    private readonly routes;
    private translations;
    constructor(loader: I18NRouterLoader, router: Router);
    init(useLocalizedRoutes?: boolean): void;
    changeLanguage(languageCode: string): void;
    getTranslation(key: string): string;
    translateRoutes(routes: Routes, moduleKey?: string): Routes;
    private interpolateRoute(path);
    private translateRoute(route, property, moduleKey?);
}
export declare function provideChildRoutes(i18nRouter: I18NRouterService, routes: Routes, moduleKey: string): Routes;
