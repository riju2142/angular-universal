import * as _ from 'lodash';
var I18NRouterConfigLoader = (function () {
    function I18NRouterConfigLoader(config, routes, group) {
        if (group === void 0) { group = 'routes'; }
        this.config = config;
        this.routes = routes;
        this.group = group;
    }
    I18NRouterConfigLoader.prototype.loadTranslations = function () {
        return Promise.resolve(undefined);
    };
    I18NRouterConfigLoader.prototype.getRoutes = function () {
        return _.map(this.routes, _.cloneDeep);
    };
    I18NRouterConfigLoader.prototype.getTranslations = function () {
        if (!this.config)
            throw new Error('No [config] specified!');
        if (!this.config.getSettings())
            return undefined;
        return this.config.getSettings(this.group);
    };
    return I18NRouterConfigLoader;
}());
export { I18NRouterConfigLoader };
