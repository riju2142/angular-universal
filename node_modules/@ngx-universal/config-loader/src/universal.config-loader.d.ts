import { ConfigLoader } from '@ngx-config/core';
export declare class UniversalConfigLoader implements ConfigLoader {
    private readonly platformId;
    private readonly serverLoader;
    private readonly browserLoader;
    constructor(platformId: any, serverLoader: ConfigLoader, browserLoader: ConfigLoader);
    loadSettings(): any;
}
